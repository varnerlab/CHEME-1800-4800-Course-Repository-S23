# include -
include("Include.jl")

# develop a function that returns the index of the stduent when given the sid and netid
# what should the return type be?
function find_student_index(stduents::Array{Student}; sid::Int64=0, netid::String="abc123")
end


# Build an array of 1000 student models using the 
# build_student_array(number_of_students::Int64; max_sid::Int64 = 10000)::Array{Student,1} function


